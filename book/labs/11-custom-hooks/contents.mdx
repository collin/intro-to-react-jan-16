import { Objectives } from "Objectives";

export const title = "Custom Hooks";

<Objectives>
  In this lab, you will:

- Write a custom hook to use the `requestAnimationFrame` API
- Write a custom hook to wrap the `fetch` API
- Write a custom hook to respond to resizing of DOM nodes

</Objectives>
