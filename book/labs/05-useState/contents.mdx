import { Objectives } from "Objectives";

export const title = "useState";
export const noCommonPreview = true;

<Objectives>
  In this lab, you will:

- practice using the `useState` hook with simple values (strings, numbers, booleans)
- practice using the `useState` hook with complex values (objects, arrays)
- explore unintuitive quirks of `useState`

</Objectives>
