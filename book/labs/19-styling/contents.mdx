import { Objectives } from "Objectives";

export const title = "styling / component libraries";

<Objectives>
  In this lab, you will:

- use css modules to organize your styles
- use the `classnames` library to conditionally apply styles
- use a `css-in-js` library to style your components
- use `tailwindcss` to style your components
- use a component library to style your components

</Objectives>
