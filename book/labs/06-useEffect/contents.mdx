import { Objectives } from "Objectives";

export const title = "useEffect";
export const noCommonPreview = true;

<Objectives>
  In this lab, you will:

- Use `useEffect` and the `fetch` API to fetch data
- Use `useEffect` to interact with the DOM

</Objectives>
