import { Objectives } from "Objectives";

export const title = "useContext";

<Objectives>
  In this lab, you will:

- Create and consume data with `createContext`/`useContext`
- Implement a common `<Provider>` and custom `useContext` hook pattern
- Explore how updating context values causes re-renders

</Objectives>
