import { Objectives } from "Objectives";

export const title = "props";
export const noCommonPreview = true;

<Objectives>
  In this lab, you will:

- pass simple probs into components
- use the boolean property syntax
- use the `...spread` operator props syntax
- use `props.children` to render children components
- set the classname of an html element using the `className` prop
- use the `style` prop to set the style of an html element

</Objectives>
