import { Objectives } from "Objectives";

export const title = "useRef";

<Objectives>
  In this lab, you will:

- Practice using the `useRef` hook to reference DOM elements
- Practice using `useRef` with `useEffect` to use DOM elements in an effect
- Practice using `useRef` to store a value that persists across renders

</Objectives>
