import { Objectives } from "Objectives";

export const title = "@testing-library/react";

<Objectives>
  In this lab, you will:

- Setup `vitest` to run tests with `@testing-library/react`
- Test user interactions with React components using `@testing-library/react`
- Test data fetching with `@testing-library/react`
- Test React components that use React Router with `@testing-library/react`
- Test a `<form>` with `@testing-library/react`

</Objectives>
