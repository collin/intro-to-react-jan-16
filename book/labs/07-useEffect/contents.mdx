import { Objectives } from "Objectives";

export const title = "useEffect";

<Objectives>
  In this lab, you will:

- Subscribe to an event with `useEffect`
- Unsubscribe from an event with `useEffect`
- Use the dependency array to control an effect is called
- Use `useEffect` to fetch data from an API

</Objectives>
